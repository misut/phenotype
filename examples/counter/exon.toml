[package]
name = "counter"
version = "0.1.0"
description = "phenotype counter example with reactive state"
type = "bin"
standard = 23
platforms = [{ os = "wasi" }]

[dependencies]
"github.com/misut/cppx" = "1.9.0"

[dependencies.path]
phenotype = "../.."
