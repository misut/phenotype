[package]
name = "glass_showcase_shared"
version = "0.1.0"
description = "Shared deterministic glass showcase model for phenotype examples"
type = "lib"
standard = 23
platforms = [
    { os = "linux", arch = "x86_64" },
    { os = "macos", arch = "aarch64" },
    { os = "windows", arch = "x86_64" },
]

[dependencies]
"github.com/misut/jsoncpp" = "0.1.4"

[sync]
cmake-in-root = false
