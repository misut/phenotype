[package]
name = "phenotype_cli"
version = "0.1.0"
description = "Host CLI for phenotype packaging, diagnostics, and artifact inspection"
type = "bin"
standard = 23
platforms = [
    { os = "macos", arch = "aarch64" },
    { os = "windows", arch = "x86_64" },
]

[dependencies]
"github.com/misut/cppx" = "1.13.0"

[sync]
cmake-in-root = false
